# 🏗️ Архитектура и Развитие Проекта Agent SPC

Этот документ содержит подробный технический обзор архитектуры, функциональности и особенностей реализации проекта **Agent SPC**, а также гипотезы по его дальнейшему развитию.

---

## 1. 🏛️ Архитектура Проекта

Проект построен на микросервисной архитектуре с использованием **LangGraph** для управления состоянием агента и **Flask** для предоставления API.

### 🧩 Основные Модули

#### 1.1. Agent Core (`agent/`)
Ядро системы, отвечающее за логику принятия решений.
- **StateGraph (`agent/state_graph/`)**: Использует библиотеку `langgraph` для создания направленного циклического графа (DAG) работы агента.
  - *Nodes*: `show_tools` (выбор инструментов), `run_tool` (выполнение).
  - *Edges*: Условные переходы на основе `should_continue`.
- **Memory (`agent/memory/`)**: Управление состоянием (`State`). Хранит историю сообщений, результаты выполнения инструментов и счетчики токенов.
- **Parsing (`agent/parsing/`)**: Модули для обработки ответов LLM.
  - Парсинг JSON-блоков (`action`, `action_input`).
  - Очистка Markdown разметки.
- **Prompts (`agent/prompts/`)**: Хранилище системных промптов для разных задач (классификация, поиск, оценка).

#### 1.2. Tools Subsystem (`tools/`)
Набор инструментов, доступных агенту.
- **Integration**: `bitbucket.py` (API клиент), `git.py` (Git операции).
- **Search Engine**: `search_content.py` — гибридный поиск.
- **Generators**: `gen_main.py` — создание документации.
- **Evaluators**:
  - `evalution_code/`: Статический анализ кода (pylint, nbqa, sqlfluff).
  - `evalution_repo/`: Анализ структуры и качества оформления (эвристики + LLM).

#### 1.3. Infrastructure
- **Entry Points**: `main.py` (CLI), `run_mlflow_server.py` (API).
- **Configuration**: `.env` файл, `settings.py`.

---

## 2. 🔑 Ключевые Технологии и Модели

### 🧠 Large Language Model (LLM)
- **Модель**: `GigaChat-2-Max` (SberDevices).
- **Роль**: Понимание намерений пользователя, генерация текста, принятие решений (ReAct паттерн), оценка семантического соответствия.
- **Особенности**: Реализован кастомный враппер `CustomGigaChat` с механизмом **Retry** (до 10 попыток) и обработкой `BlackList` ошибок.

### 🔎 Embeddings & Search
- **Модель**: `BGEM3FlagModel` (BAAI/bge-m3).
- **Тип**: Гибридная модель (Dense + Sparse + ColBERT).
- **Реализация**:
  1. **Dense Retrieval**: Семантический поиск по плотным векторам.
  2. **Lexical Retrieval (Sparse)**: Поиск по ключевым словам (BM25-like).
  3. **Multi-Vector (ColBERT)**: Финальное переранжирование (Re-ranking) для максимальной точности.
- **Кэширование**: Векторы сохраняются в `pickle` файл для ускорения повторных запусков.

### ⚙️ Frameworks
- **LangChain / LangGraph**: Оркестрация агента.
- **Flask**: REST API интерфейс.
- **MLflow**: Трекинг экспериментов и метрик (в `run_mlflow_server.py`).

---

## 3. 🛠️ Обзор Функционала и Реализации

### 3.1. Семантический Поиск (`search_content`)
Агент не просто ищет по ключевым словам, а понимает смысл.
- **Реализация**: Загрузка `.md` файлов из базы знаний -> Векторизация (BGE-M3) -> Поиск топ-K кандидатов -> Генерация ответа LLM на основе найденных чанков (RAG).

### 3.2. Оценка Качества Кода (`rate_repository`)
Агент выступает в роли строгого Code Reviewer.
- **Python**: Запуск `pylint` с кастомным конфигом. Парсинг stdout для извлечения оценки (0-10) и списка ошибок.
- **Jupyter**: Использование `nbqa` для запуска линтеров на ноутбуках.
- **SQL**: Проверка синтаксиса и стиля через `sqlfluff` с определением диалекта (Hive, SparkSQL, Oracle).

### 3.3. Оценка Оформления (`awerage_repo`)
Уникальная двухуровневая система оценки (0-26 баллов).
1. **Структурный анализ (Эвристики)**: Проверка наличия `.gitignore`, правильности именования папок/файлов (snake_case), наличия `requirements.txt`.
2. **Семантический анализ (LLM)**: GigaChat читает `README.md` и оценивает:
   - Понятность описания.
   - Наличие контактов ответственных.
   - Описание скриптов и данных.

### 3.4. Генерация Документации (`gen_readme`)
Автоматическое создание `README.md`.
- **Логика**: Клонирование репо -> Анализ структуры файлов -> Чтение первых строк кода (preview) -> Генерация описания через LLM -> Вставка плейсхолдеров `<span style="color:red">ЗАПОЛНИТЬ</span>` для ручного ввода.

---

## 4. ✨ Особенности Реализации

1. **State Management**: Класс `State` накапливает контекст выполнения. Агент "помнит", что он делал на предыдущих шагах (история инструментов), и использует это для принятия следующего решения.
2. **Token Tracking**: Система считает токены (prompt + completion) для каждого запроса и сохраняет статистику в JSON, что позволяет контролировать расходы.
3. **Robustness (Устойчивость)**:
   - Декоратор `ignore_error` для повторных попыток запросов к API.
   - Fallback механизмы: если LLM недоступна для оценки README, включаются эвристические алгоритмы (поиск ключевых слов).
4. **Модульность**: Каждый инструмент — это отдельный класс, наследующий базовый конфиг. Это позволяет легко добавлять новые тулы.

---

## 5. 🚀 Гипотезы по Улучшению (Roadmap)

Ниже представлены гипотезы для развития проекта от текущего состояния (MVP+) к полноценному Enterprise решению.

### 🧪 Гипотеза 1: Multi-Agent Architecture (Специализация)
**Проблема**: Сейчас один агент пытается быть "мастером на все руки". При сложных задачах контекст размывается.
**Решение**: Разделить на специализированных агентов:
- *Researcher Agent*: Ищет информацию и документацию.
- *Coder Agent*: Пишет и правит код.
- *Reviewer Agent*: Только оценивает и критикует.
- *Manager Agent*: Оркестрирует их работу.
**Ожидаемый эффект**: Повышение качества выполнения сложных, многосоставных задач на 30-40%.

### 🧪 Гипотеза 2: GraphRAG (Контекстный поиск кода)
**Проблема**: Текущий RAG ищет по текстовым чанкам. Он не понимает зависимости в коде (кто вызывает эту функцию? где определен этот класс?).
**Решение**: Построить граф знаний (Knowledge Graph) по кодовой базе. Использовать GraphRAG для навигации по зависимостям.
**Ожидаемый эффект**: Агент сможет отвечать на вопросы типа "Как изменение в функции A повлияет на модуль B?".

### 🧪 Гипотеза 3: Auto-Fixing (Агент-исправитель)
**Проблема**: Сейчас агент только *указывает* на ошибки (linter errors), но не исправляет их.
**Решение**: Реализовать цикл: `Linter -> Error Log -> LLM (Fix Code) -> Linter (Verify)`. Агент будет автоматически создавать Pull Request с исправлениями стиля и простых багов.
**Ожидаемый эффект**: Сокращение времени разработчиков на рутину (formatting, imports, pep8) до 0.

### 🧪 Гипотеза 4: CI/CD Integration (BitBucket Webhooks)
**Проблема**: Агент запускается вручную.
**Решение**: Интегрировать агента в CI пайплайн. При создании Pull Request агент автоматически:
1. Оценивает качество кода.
2. Проверяет документацию.
3. Оставляет комментарии прямо в строках кода в BitBucket.
**Ожидаемый эффект**: Автоматический контроль качества (Quality Gate) без участия человека.

### 🧪 Гипотеза 5: Active Learning (Обратная связь)
**Проблема**: Агент не учится на своих ошибках.
**Решение**: Добавить механизм Feedback. Если пользователь помечает ответ как "плохой", сохранять этот кейс. Использовать эти данные для дообучения (Fine-tuning) GigaChat или оптимизации промптов (DSPy).
**Ожидаемый эффект**: Постоянный рост качества ответов специфично для доменной области банка.
